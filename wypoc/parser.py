#!/usr/bin/env python3.8
# @generated by pegen from /home/andrew/projects/amarach/wypoc/wypoc/wyrm.gram

import ast
import sys
import tokenize

from typing import Any, Optional

from pegen.parser import memoize, memoize_left_rec, logger, Parser

from wypoc.actions import (
    fold_left, make_assignment_stmt, make_static_import, make_string_literal,
    make_tuple, make_type_expr, fold_postfix, fold_index_target, names, spans,
    tail, tok_pos, with_pos,
)
from wypoc.ast_nodes import (
    Program, Pass, ExprStmt, Return, Yield, ElifClause, If, While, For,
    Continue, Break, NameTarget,
    AttrTarget, Assign, VarTarget, VarDecl, StaticDecl, TypeExpr, WithSimple,
    WithBinding, WithBlock, Import, ImportItem, FromImport, Param,
    VarPositional, VarKeyword, FnDef, CoDef, ClassDef, SlotOption, SlotDef,
    Num, Str, Char, Bool, Symbol, EllipsisExpr, Name, AstRef, ThisRef,
    SuperCall, Defined,
    Lambda, Do, Array, Pair, Tuple, DictEntry, Dict, MessageTupleExpr,
    Kwarg, SpreadPos, SpreadKw, UnaryOp, BinOp, SetIfUnset, Call, Index,
    Attr, Message, Scope, Defer, Try, Catch, TypeCheck, Decorator, Decorated,
)

# Keywords and soft keywords are listed at the end of the parser definition.
class GeneratedParser(Parser):

    @memoize
    def start(self) -> Optional[object]:
        # start: program $
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (p := self.program())
            and
            (self.expect('ENDMARKER'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return with_pos ( p , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def program(self) -> Optional[object]:
        # program: stmt_item* NEWLINE*
        # nullable=True
        mark = self._mark()
        if (
            (stmts := self._loop0_1(),)
            and
            (self._loop0_2(),)
        ):
            return Program ( [s for group in stmts for s in group] );
        self._reset(mark)
        return None;

    @memoize
    def block(self) -> Optional[object]:
        # block: ":" NEWLINE INDENT stmt_item* DEDENT | ":" simple_stmt_line | "{" NEWLINE* brace_stmt_item* "}"
        mark = self._mark()
        if (
            (self.expect(":"))
            and
            (self.expect('NEWLINE'))
            and
            (self.expect('INDENT'))
            and
            (stmts := self._loop0_3(),)
            and
            (self.expect('DEDENT'))
        ):
            return [s for group in stmts for s in group];
        self._reset(mark)
        if (
            (self.expect(":"))
            and
            (stmts := self.simple_stmt_line())
        ):
            return stmts;
        self._reset(mark)
        if (
            (self.expect("{"))
            and
            (self._loop0_4(),)
            and
            (stmts := self._loop0_5(),)
            and
            (self.expect("}"))
        ):
            return [s for group in stmts for s in group];
        self._reset(mark)
        return None;

    @memoize
    def stmt_item(self) -> Optional[object]:
        # stmt_item: compound_stmt NEWLINE? | simple_stmt_line
        mark = self._mark()
        if (
            (s := self.compound_stmt())
            and
            (self.expect('NEWLINE'),)
        ):
            return [s];
        self._reset(mark)
        if (
            (simple_stmt_line := self.simple_stmt_line())
        ):
            return simple_stmt_line;
        self._reset(mark)
        return None;

    @memoize
    def brace_stmt_item(self) -> Optional[object]:
        # brace_stmt_item: compound_stmt NEWLINE* | simple_stmt NEWLINE+ | simple_stmt
        mark = self._mark()
        if (
            (s := self.compound_stmt())
            and
            (self._loop0_6(),)
        ):
            return [s];
        self._reset(mark)
        if (
            (first := self.simple_stmt())
            and
            (self._loop1_7())
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.simple_stmt())
        ):
            return [first];
        self._reset(mark)
        return None;

    @memoize
    def simple_stmt_line(self) -> Optional[object]:
        # simple_stmt_line: simple_stmt ";" simple_stmt_line | simple_stmt ";" NEWLINE | simple_stmt ";" | simple_stmt NEWLINE | simple_stmt
        mark = self._mark()
        if (
            (first := self.simple_stmt())
            and
            (self.expect(";"))
            and
            (rest := self.simple_stmt_line())
        ):
            return [first] + rest;
        self._reset(mark)
        if (
            (first := self.simple_stmt())
            and
            (self.expect(";"))
            and
            (self.expect('NEWLINE'))
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.simple_stmt())
            and
            (self.expect(";"))
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.simple_stmt())
            and
            (self.expect('NEWLINE'))
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.simple_stmt())
        ):
            return [first];
        self._reset(mark)
        return None;

    @memoize
    def simple_stmt(self) -> Optional[object]:
        # simple_stmt: with_stmt_simple | static_stmt | var_stmt | assignment_stmt | from_import_stmt | import_stmt | return_stmt | yield_stmt | pass_stmt | continue_stmt | break_stmt | expr_stmt
        mark = self._mark()
        if (
            (with_stmt_simple := self.with_stmt_simple())
        ):
            return with_stmt_simple;
        self._reset(mark)
        if (
            (static_stmt := self.static_stmt())
        ):
            return static_stmt;
        self._reset(mark)
        if (
            (var_stmt := self.var_stmt())
        ):
            return var_stmt;
        self._reset(mark)
        if (
            (assignment_stmt := self.assignment_stmt())
        ):
            return assignment_stmt;
        self._reset(mark)
        if (
            (from_import_stmt := self.from_import_stmt())
        ):
            return from_import_stmt;
        self._reset(mark)
        if (
            (import_stmt := self.import_stmt())
        ):
            return import_stmt;
        self._reset(mark)
        if (
            (return_stmt := self.return_stmt())
        ):
            return return_stmt;
        self._reset(mark)
        if (
            (yield_stmt := self.yield_stmt())
        ):
            return yield_stmt;
        self._reset(mark)
        if (
            (pass_stmt := self.pass_stmt())
        ):
            return pass_stmt;
        self._reset(mark)
        if (
            (continue_stmt := self.continue_stmt())
        ):
            return continue_stmt;
        self._reset(mark)
        if (
            (break_stmt := self.break_stmt())
        ):
            return break_stmt;
        self._reset(mark)
        if (
            (expr_stmt := self.expr_stmt())
        ):
            return expr_stmt;
        self._reset(mark)
        return None;

    @memoize
    def compound_stmt(self) -> Optional[object]:
        # compound_stmt: decorator compound_stmt | if_stmt | while_stmt | for_stmt | fn_def | co_def | class_def | with_block | defer_stmt
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (dec := self.decorator())
            and
            (s := self.compound_stmt())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Decorated ( dec , s , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (if_stmt := self.if_stmt())
        ):
            return if_stmt;
        self._reset(mark)
        if (
            (while_stmt := self.while_stmt())
        ):
            return while_stmt;
        self._reset(mark)
        if (
            (for_stmt := self.for_stmt())
        ):
            return for_stmt;
        self._reset(mark)
        if (
            (fn_def := self.fn_def())
        ):
            return fn_def;
        self._reset(mark)
        if (
            (co_def := self.co_def())
        ):
            return co_def;
        self._reset(mark)
        if (
            (class_def := self.class_def())
        ):
            return class_def;
        self._reset(mark)
        if (
            (with_block := self.with_block())
        ):
            return with_block;
        self._reset(mark)
        if (
            (defer_stmt := self.defer_stmt())
        ):
            return defer_stmt;
        self._reset(mark)
        return None;

    @memoize
    def decorator(self) -> Optional[object]:
        # decorator: '@' NAME "(" arg_list? ")" NEWLINE? | '@' NAME NEWLINE?
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('@'))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (args := self.arg_list(),)
            and
            (self.expect(")"))
            and
            (self.expect('NEWLINE'),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Decorator ( n . string , args or [] , True , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('@'))
            and
            (n := self.name())
            and
            (self.expect('NEWLINE'),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Decorator ( n . string , [] , False , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def pass_stmt(self) -> Optional[object]:
        # pass_stmt: 'pass'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('pass'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Pass ( pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def continue_stmt(self) -> Optional[object]:
        # continue_stmt: 'continue'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('continue'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Continue ( pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def break_stmt(self) -> Optional[object]:
        # break_stmt: 'break'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('break'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Break ( pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def expr_stmt(self) -> Optional[object]:
        # expr_stmt: expression
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ExprStmt ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def return_stmt(self) -> Optional[object]:
        # return_stmt: 'return' expression | 'return'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('return'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Return ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('return'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Return ( None , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def yield_stmt(self) -> Optional[object]:
        # yield_stmt: 'yield' 'from' expression | 'yield' expression | 'yield'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('yield'))
            and
            (self.expect('from'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( e , True , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('yield'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('yield'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( None , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def if_stmt(self) -> Optional[object]:
        # if_stmt: 'if' expression block elif_clause* else_clause?
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('if'))
            and
            (cond := self.expression())
            and
            (body := self.block())
            and
            (elifs := self._loop0_8(),)
            and
            (orelse := self.else_clause(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return If ( cond , body , elifs , orelse , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def elif_clause(self) -> Optional[object]:
        # elif_clause: 'elif' expression block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('elif'))
            and
            (cond := self.expression())
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ElifClause ( cond , body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def else_clause(self) -> Optional[object]:
        # else_clause: 'else' block
        mark = self._mark()
        if (
            (self.expect('else'))
            and
            (body := self.block())
        ):
            return body;
        self._reset(mark)
        return None;

    @memoize
    def while_stmt(self) -> Optional[object]:
        # while_stmt: 'while' expression block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('while'))
            and
            (cond := self.expression())
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return While ( cond , body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def for_stmt(self) -> Optional[object]:
        # for_stmt: 'for' NAME 'in' expression block else_clause?
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('for'))
            and
            (n := self.name())
            and
            (self.expect('in'))
            and
            (it := self.expression())
            and
            (body := self.block())
            and
            (orelse := self.else_clause(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return For ( n . string , it , body , orelse , var_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def defer_stmt(self) -> Optional[object]:
        # defer_stmt: 'defer' "on" "error" block | 'defer' block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('defer'))
            and
            (self.expect("on"))
            and
            (self.expect("error"))
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Defer ( True , body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('defer'))
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Defer ( False , body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def var_stmt(self) -> Optional[object]:
        # var_stmt: 'var' var_target (("," var_target))* [("=" expr_list)]
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('var'))
            and
            (first := self.var_target())
            and
            (rest := self._loop0_9(),)
            and
            (vals := self._tmp_10(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return VarDecl ( [first] + tail ( rest ) , vals [1] if vals else None , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def var_target(self) -> Optional[object]:
        # var_target: NAME [(":" ann_type)]
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (n := self.name())
            and
            (ty := self._tmp_11(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return VarTarget ( n . string , ty [1] if ty else None , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def assignment_stmt(self) -> Optional[object]:
        # assignment_stmt: target_list assign_op expr_list
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (targets := self.target_list())
            and
            (op := self.assign_op())
            and
            (vals := self.expr_list())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return make_assignment_stmt ( targets , op , vals , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def target_list(self) -> Optional[object]:
        # target_list: target (("," target))*
        mark = self._mark()
        if (
            (first := self.target())
            and
            (rest := self._loop0_12(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def target(self) -> Optional[object]:
        # target: target_base target_index_suffix*
        mark = self._mark()
        if (
            (base := self.target_base())
            and
            (idxs := self._loop0_13(),)
        ):
            return fold_index_target ( base , idxs );
        self._reset(mark)
        return None;

    @memoize
    def target_base(self) -> Optional[object]:
        # target_base: 'this' (("." NAME))+ | NAME (("." NAME))+ | NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (t := self.expect('this'))
            and
            (rest := self._loop1_14())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return AttrTarget ( ThisRef ( tok_pos ( t ) ) , names ( tail ( rest ) ) , base_pos = tok_pos ( t ) , attrs_pos = spans ( tail ( rest ) ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (n := self.name())
            and
            (rest := self._loop1_15())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return AttrTarget ( n . string , names ( tail ( rest ) ) , base_pos = tok_pos ( n ) , attrs_pos = spans ( tail ( rest ) ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (name := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return NameTarget ( name . string , name_pos = tok_pos ( name ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def target_index_suffix(self) -> Optional[object]:
        # target_index_suffix: "[" expression "]"
        mark = self._mark()
        if (
            (self.expect("["))
            and
            (e := self.expression())
            and
            (self.expect("]"))
        ):
            return e;
        self._reset(mark)
        return None;

    @memoize
    def assign_op(self) -> Optional[object]:
        # assign_op: "?=" | ":=" | "="
        mark = self._mark()
        if (
            (self.expect("?="))
        ):
            return '?=';
        self._reset(mark)
        if (
            (self.expect(":="))
        ):
            return ':=';
        self._reset(mark)
        if (
            (self.expect("="))
        ):
            return '=';
        self._reset(mark)
        return None;

    @memoize
    def static_stmt(self) -> Optional[object]:
        # static_stmt: 'static' NAME [(":" ann_type)] [("=" assign_expr)]
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('static'))
            and
            (n := self.name())
            and
            (ty := self._tmp_16(),)
            and
            (dflt := self._tmp_17(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return StaticDecl ( n . string , ty [1] if ty else None , dflt [1] if dflt else None , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def type_expr(self) -> Optional[object]:
        # type_expr: NAME (("::" NAME))*
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (first := self.name())
            and
            (rest := self._loop0_18(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return make_type_expr ( [first] + tail ( rest ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def ann_type(self) -> Optional[object]:
        # ann_type: type_expr (("|" type_expr))*
        mark = self._mark()
        if (
            (first := self.type_expr())
            and
            (self._loop0_19(),)
        ):
            return first;
        self._reset(mark)
        return None;

    @memoize
    def with_stmt_simple(self) -> Optional[object]:
        # with_stmt_simple: 'with' NAME [(":" ann_type)] "=" assign_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('with'))
            and
            (n := self.name())
            and
            (ty := self._tmp_20(),)
            and
            (self.expect("="))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return WithSimple ( n . string , ty [1] if ty else None , e , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def with_block(self) -> Optional[object]:
        # with_block: 'with' ":" NEWLINE INDENT with_binding_list DEDENT
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('with'))
            and
            (self.expect(":"))
            and
            (self.expect('NEWLINE'))
            and
            (self.expect('INDENT'))
            and
            (bindings := self.with_binding_list())
            and
            (self.expect('DEDENT'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return WithBlock ( bindings , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def with_binding_list(self) -> Optional[object]:
        # with_binding_list: with_binding NEWLINE with_binding_list | with_binding NEWLINE | with_binding
        mark = self._mark()
        if (
            (first := self.with_binding())
            and
            (self.expect('NEWLINE'))
            and
            (rest := self.with_binding_list())
        ):
            return [first] + rest;
        self._reset(mark)
        if (
            (first := self.with_binding())
            and
            (self.expect('NEWLINE'))
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.with_binding())
        ):
            return [first];
        self._reset(mark)
        return None;

    @memoize
    def with_binding(self) -> Optional[object]:
        # with_binding: NAME [(":" ann_type)] "=" assign_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (n := self.name())
            and
            (ty := self._tmp_21(),)
            and
            (self.expect("="))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return WithBinding ( n . string , ty [1] if ty else None , e , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def import_stmt(self) -> Optional[object]:
        # import_stmt: 'import' 'static' import_body | 'import' import_body
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('import'))
            and
            (self.expect('static'))
            and
            (body := self.import_body())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return with_pos ( make_static_import ( body ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('import'))
            and
            (body := self.import_body())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return with_pos ( body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def import_body(self) -> Optional[object]:
        # import_body: module_path "::" "*" "except" except_names | module_path "::" "*" | module_path "::" "(" import_item_list ")" | module_path "as" NAME | module_path
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (m := self.module_path())
            and
            (self.expect("::"))
            and
            (self.expect("*"))
            and
            (self.expect("except"))
            and
            (ex := self.except_names())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Import ( names ( m ) , wildcard = True , except_names = names ( ex ) , path_pos = spans ( m ) , except_names_pos = spans ( ex ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (m := self.module_path())
            and
            (self.expect("::"))
            and
            (self.expect("*"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Import ( names ( m ) , wildcard = True , path_pos = spans ( m ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (m := self.module_path())
            and
            (self.expect("::"))
            and
            (self.expect("("))
            and
            (items := self.import_item_list())
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Import ( names ( m ) , items = items , path_pos = spans ( m ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (m := self.module_path())
            and
            (self.expect("as"))
            and
            (alias := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Import ( names ( m ) , alias = alias . string , path_pos = spans ( m ) , alias_pos = tok_pos ( alias ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (m := self.module_path())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Import ( names ( m ) , path_pos = spans ( m ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def module_path(self) -> Optional[object]:
        # module_path: NAME (("::" NAME))*
        mark = self._mark()
        if (
            (first := self.name())
            and
            (rest := self._loop0_22(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def except_names(self) -> Optional[object]:
        # except_names: "(" name_list ")" | NAME
        mark = self._mark()
        if (
            (self.expect("("))
            and
            (ns := self.name_list())
            and
            (self.expect(")"))
        ):
            return ns;
        self._reset(mark)
        if (
            (n := self.name())
        ):
            return [n];
        self._reset(mark)
        return None;

    @memoize
    def import_item(self) -> Optional[object]:
        # import_item: NAME "as" NAME | NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (n := self.name())
            and
            (self.expect("as"))
            and
            (alias := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ImportItem ( n . string , alias . string , name_pos = tok_pos ( n ) , alias_pos = tok_pos ( alias ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (n := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ImportItem ( n . string , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def import_item_list(self) -> Optional[object]:
        # import_item_list: import_item (("," import_item))*
        mark = self._mark()
        if (
            (first := self.import_item())
            and
            (rest := self._loop0_23(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def from_import_stmt(self) -> Optional[object]:
        # from_import_stmt: 'from' module_path 'import' name_list
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('from'))
            and
            (m := self.module_path())
            and
            (self.expect('import'))
            and
            (ns := self.name_list())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return FromImport ( names ( m ) , names ( ns ) , path_pos = spans ( m ) , names_pos = spans ( ns ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def name_list(self) -> Optional[object]:
        # name_list: NAME (("," NAME))*
        mark = self._mark()
        if (
            (first := self.name())
            and
            (rest := self._loop0_24(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def fn_def(self) -> Optional[object]:
        # fn_def: 'fn' class_target NAME "(" param_list? ")" [("->" ann_type)] block | 'fn' NAME "(" param_list? ")" [("->" ann_type)] block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('fn'))
            and
            (cls := self.class_target())
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (params := self.param_list(),)
            and
            (self.expect(")"))
            and
            (ret := self._tmp_25(),)
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return FnDef ( names ( cls ) , n . string , params or [] , ret [1] if ret else None , body , class_target_pos = spans ( cls ) , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('fn'))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (params := self.param_list(),)
            and
            (self.expect(")"))
            and
            (ret := self._tmp_26(),)
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return FnDef ( None , n . string , params or [] , ret [1] if ret else None , body , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def class_target(self) -> Optional[object]:
        # class_target: "[" NAME (("," NAME))* "]"
        mark = self._mark()
        if (
            (self.expect("["))
            and
            (first := self.name())
            and
            (rest := self._loop0_27(),)
            and
            (self.expect("]"))
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def param_list(self) -> Optional[object]:
        # param_list: param (("," param))*
        mark = self._mark()
        if (
            (first := self.param())
            and
            (rest := self._loop0_28(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def param(self) -> Optional[object]:
        # param: var_positional | var_keyword | plain_param
        mark = self._mark()
        if (
            (var_positional := self.var_positional())
        ):
            return var_positional;
        self._reset(mark)
        if (
            (var_keyword := self.var_keyword())
        ):
            return var_keyword;
        self._reset(mark)
        if (
            (plain_param := self.plain_param())
        ):
            return plain_param;
        self._reset(mark)
        return None;

    @memoize
    def plain_param(self) -> Optional[object]:
        # plain_param: NAME [(":" ann_type)] [("=" assign_expr)]
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (n := self.name())
            and
            (ty := self._tmp_29(),)
            and
            (dflt := self._tmp_30(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Param ( n . string , ty [1] if ty else None , dflt [1] if dflt else None , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def var_positional(self) -> Optional[object]:
        # var_positional: "*" NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("*"))
            and
            (n := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return VarPositional ( n . string , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def var_keyword(self) -> Optional[object]:
        # var_keyword: "**" NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("**"))
            and
            (n := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return VarKeyword ( n . string , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def co_params(self) -> Optional[object]:
        # co_params: "<-" ann_type "," param_list | "<-" ann_type | param_list
        mark = self._mark()
        if (
            (self.expect("<-"))
            and
            (t := self.ann_type())
            and
            (self.expect(","))
            and
            (p := self.param_list())
        ):
            return ( t , p );
        self._reset(mark)
        if (
            (self.expect("<-"))
            and
            (t := self.ann_type())
        ):
            return ( t , [] );
        self._reset(mark)
        if (
            (p := self.param_list())
        ):
            return ( None , p );
        self._reset(mark)
        return None;

    @memoize
    def co_def(self) -> Optional[object]:
        # co_def: 'co' class_target NAME "(" co_params? ")" [("->" ann_type)] block | 'co' NAME "(" co_params? ")" [("->" ann_type)] block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('co'))
            and
            (cls := self.class_target())
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (cp := self.co_params(),)
            and
            (self.expect(")"))
            and
            (ret := self._tmp_31(),)
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return CoDef ( names ( cls ) , n . string , cp [1] if cp else [] , cp [0] if cp else None , ret [1] if ret else None , body , class_target_pos = spans ( cls ) , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('co'))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (cp := self.co_params(),)
            and
            (self.expect(")"))
            and
            (ret := self._tmp_32(),)
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return CoDef ( None , n . string , cp [1] if cp else [] , cp [0] if cp else None , ret [1] if ret else None , body , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def class_def(self) -> Optional[object]:
        # class_def: 'class' NAME "(" arg_list? ")" class_block | 'class' NAME class_block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('class'))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (bases := self.arg_list(),)
            and
            (self.expect(")"))
            and
            (body := self.class_block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ClassDef ( n . string , bases or [] , body [0] , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('class'))
            and
            (n := self.name())
            and
            (body := self.class_block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ClassDef ( n . string , [] , body [0] , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def class_block(self) -> Optional[object]:
        # class_block: ":" NEWLINE INDENT class_member_item* DEDENT | "{" NEWLINE* class_member_brace* "}"
        mark = self._mark()
        if (
            (self.expect(":"))
            and
            (self.expect('NEWLINE'))
            and
            (self.expect('INDENT'))
            and
            (members := self._loop0_33(),)
            and
            (self.expect('DEDENT'))
        ):
            return ( members , );
        self._reset(mark)
        if (
            (self.expect("{"))
            and
            (self._loop0_34(),)
            and
            (members := self._loop0_35(),)
            and
            (self.expect("}"))
        ):
            return ( members , );
        self._reset(mark)
        return None;

    @memoize
    def class_member_brace(self) -> Optional[object]:
        # class_member_brace: fn_def NEWLINE* | co_def NEWLINE* | slot_def NEWLINE* | static_stmt NEWLINE*
        mark = self._mark()
        if (
            (m := self.fn_def())
            and
            (self._loop0_36(),)
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.co_def())
            and
            (self._loop0_37(),)
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.slot_def())
            and
            (self._loop0_38(),)
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.static_stmt())
            and
            (self._loop0_39(),)
        ):
            return m;
        self._reset(mark)
        return None;

    @memoize
    def class_member_item(self) -> Optional[object]:
        # class_member_item: fn_def NEWLINE? | co_def NEWLINE? | slot_def NEWLINE | slot_def | static_stmt NEWLINE?
        mark = self._mark()
        if (
            (m := self.fn_def())
            and
            (self.expect('NEWLINE'),)
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.co_def())
            and
            (self.expect('NEWLINE'),)
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.slot_def())
            and
            (self.expect('NEWLINE'))
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.slot_def())
        ):
            return m;
        self._reset(mark)
        if (
            (m := self.static_stmt())
            and
            (self.expect('NEWLINE'),)
        ):
            return m;
        self._reset(mark)
        return None;

    @memoize
    def slot_def(self) -> Optional[object]:
        # slot_def: 'slot' NAME [(":" ann_type)] [("=" assign_expr)] [('with' slot_options)]
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('slot'))
            and
            (n := self.name())
            and
            (ty := self._tmp_40(),)
            and
            (dflt := self._tmp_41(),)
            and
            (opts := self._tmp_42(),)
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SlotDef ( n . string , ty [1] if ty else None , dflt [1] if dflt else None , opts [1] if opts else None , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def slot_options(self) -> Optional[object]:
        # slot_options: ":" NEWLINE INDENT slot_option_list DEDENT | "{" slot_option_brace* "}"
        mark = self._mark()
        if (
            (self.expect(":"))
            and
            (self.expect('NEWLINE'))
            and
            (self.expect('INDENT'))
            and
            (opts := self.slot_option_list())
            and
            (self.expect('DEDENT'))
        ):
            return opts;
        self._reset(mark)
        if (
            (self.expect("{"))
            and
            (opts := self._loop0_43(),)
            and
            (self.expect("}"))
        ):
            return opts;
        self._reset(mark)
        return None;

    @memoize
    def slot_option_brace(self) -> Optional[object]:
        # slot_option_brace: slot_option ";"?
        mark = self._mark()
        if (
            (o := self.slot_option())
            and
            (self.expect(";"),)
        ):
            return o;
        self._reset(mark)
        return None;

    @memoize
    def slot_option_list(self) -> Optional[object]:
        # slot_option_list: slot_option NEWLINE slot_option_list | slot_option NEWLINE | slot_option
        mark = self._mark()
        if (
            (first := self.slot_option())
            and
            (self.expect('NEWLINE'))
            and
            (rest := self.slot_option_list())
        ):
            return [first] + rest;
        self._reset(mark)
        if (
            (first := self.slot_option())
            and
            (self.expect('NEWLINE'))
        ):
            return [first];
        self._reset(mark)
        if (
            (first := self.slot_option())
        ):
            return [first];
        self._reset(mark)
        return None;

    @memoize
    def slot_option(self) -> Optional[object]:
        # slot_option: ('setter' | 'getter') "=" 'undefined' | ('setter' | 'getter') "=" assign_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (kind := self._tmp_44())
            and
            (self.expect("="))
            and
            (self.expect('undefined'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SlotOption ( kind . string , 'undefined' , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (kind := self._tmp_45())
            and
            (self.expect("="))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SlotOption ( kind . string , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def expression(self) -> Optional[object]:
        # expression: expr_list
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (items := self.expr_list())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return items [0] if len ( items ) == 1 else Tuple ( items , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def expr_list(self) -> Optional[object]:
        # expr_list: assign_expr (("," assign_expr))*
        mark = self._mark()
        if (
            (first := self.assign_expr())
            and
            (rest := self._loop0_46(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def assign_expr(self) -> Optional[object]:
        # assign_expr: 'yield' 'from' expression | 'yield' expression | 'yield' | or_expr "?=" or_expr | catch_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('yield'))
            and
            (self.expect('from'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( e , True , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('yield'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('yield'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Yield ( None , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (a := self.or_expr())
            and
            (self.expect("?="))
            and
            (b := self.or_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SetIfUnset ( a , b , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (catch_expr := self.catch_expr())
        ):
            return catch_expr;
        self._reset(mark)
        return None;

    @memoize
    def catch_expr(self) -> Optional[object]:
        # catch_expr: try_operand 'catch' catch_handler | try_operand
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (v := self.try_operand())
            and
            (self.expect('catch'))
            and
            (h := self.catch_handler())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Catch ( v , h , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (try_operand := self.try_operand())
        ):
            return try_operand;
        self._reset(mark)
        return None;

    @memoize
    def try_operand(self) -> Optional[object]:
        # try_operand: 'try' or_expr | or_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('try'))
            and
            (e := self.or_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Try ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (or_expr := self.or_expr())
        ):
            return or_expr;
        self._reset(mark)
        return None;

    @memoize
    def catch_handler(self) -> Optional[object]:
        # catch_handler: 'return' expression | 'return' | or_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('return'))
            and
            (e := self.expression())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Return ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('return'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Return ( None , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (or_expr := self.or_expr())
        ):
            return or_expr;
        self._reset(mark)
        return None;

    @memoize
    def or_expr(self) -> Optional[object]:
        # or_expr: and_expr (('or' and_expr))*
        mark = self._mark()
        if (
            (first := self.and_expr())
            and
            (rest := self._loop0_47(),)
        ):
            return fold_left ( first , [( 'or' , e ) for _ , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def and_expr(self) -> Optional[object]:
        # and_expr: not_expr (('and' not_expr))*
        mark = self._mark()
        if (
            (first := self.not_expr())
            and
            (rest := self._loop0_48(),)
        ):
            return fold_left ( first , [( 'and' , e ) for _ , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def not_expr(self) -> Optional[object]:
        # not_expr: 'not' not_expr | comparison
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('not'))
            and
            (e := self.not_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'not' , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (comparison := self.comparison())
        ):
            return comparison;
        self._reset(mark)
        return None;

    @memoize
    def comparison(self) -> Optional[object]:
        # comparison: bitwise_or_expr 'is' 'not' type_constraint | bitwise_or_expr 'is' type_constraint | bitwise_or_expr comp_op bitwise_or_expr | bitwise_or_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (a := self.bitwise_or_expr())
            and
            (self.expect('is'))
            and
            (self.expect('not'))
            and
            (t := self.type_constraint())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'not' , TypeCheck ( a , t , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (a := self.bitwise_or_expr())
            and
            (self.expect('is'))
            and
            (t := self.type_constraint())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return TypeCheck ( a , t , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (a := self.bitwise_or_expr())
            and
            (op := self.comp_op())
            and
            (b := self.bitwise_or_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return BinOp ( op , a , b , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (bitwise_or_expr := self.bitwise_or_expr())
        ):
            return bitwise_or_expr;
        self._reset(mark)
        return None;

    @memoize
    def comp_op(self) -> Optional[object]:
        # comp_op: "<=" ">" | "<=" | ">=" | "==" | "!=" | "<" | ">" | 'in'
        mark = self._mark()
        if (
            (self.expect("<="))
            and
            (self.expect(">"))
        ):
            return '<=>';
        self._reset(mark)
        if (
            (self.expect("<="))
        ):
            return '<=';
        self._reset(mark)
        if (
            (self.expect(">="))
        ):
            return '>=';
        self._reset(mark)
        if (
            (self.expect("=="))
        ):
            return '==';
        self._reset(mark)
        if (
            (self.expect("!="))
        ):
            return '!=';
        self._reset(mark)
        if (
            (self.expect("<"))
        ):
            return '<';
        self._reset(mark)
        if (
            (self.expect(">"))
        ):
            return '>';
        self._reset(mark)
        if (
            (self.expect('in'))
        ):
            return 'in';
        self._reset(mark)
        return None;

    @memoize
    def type_constraint(self) -> Optional[object]:
        # type_constraint: type_expr (("|" type_expr))*
        mark = self._mark()
        if (
            (first := self.type_expr())
            and
            (rest := self._loop0_49(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def bitwise_or_expr(self) -> Optional[object]:
        # bitwise_or_expr: bitwise_xor_expr (("|" bitwise_xor_expr))*
        mark = self._mark()
        if (
            (first := self.bitwise_xor_expr())
            and
            (rest := self._loop0_50(),)
        ):
            return fold_left ( first , [( '|' , e ) for _ , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def bitwise_xor_expr(self) -> Optional[object]:
        # bitwise_xor_expr: bitwise_and_expr (("^" bitwise_and_expr))*
        mark = self._mark()
        if (
            (first := self.bitwise_and_expr())
            and
            (rest := self._loop0_51(),)
        ):
            return fold_left ( first , [( '^' , e ) for _ , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def bitwise_and_expr(self) -> Optional[object]:
        # bitwise_and_expr: shift_expr (("&" shift_expr))*
        mark = self._mark()
        if (
            (first := self.shift_expr())
            and
            (rest := self._loop0_52(),)
        ):
            return fold_left ( first , [( '&' , e ) for _ , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def shift_expr(self) -> Optional[object]:
        # shift_expr: additive_expr ((("<<" | ">>") additive_expr))*
        mark = self._mark()
        if (
            (first := self.additive_expr())
            and
            (rest := self._loop0_53(),)
        ):
            return fold_left ( first , [( op . string , e ) for op , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def additive_expr(self) -> Optional[object]:
        # additive_expr: mult_expr ((("+" | "-") mult_expr))*
        mark = self._mark()
        if (
            (first := self.mult_expr())
            and
            (rest := self._loop0_54(),)
        ):
            return fold_left ( first , [( op . string , e ) for op , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def mult_expr(self) -> Optional[object]:
        # mult_expr: unary_expr ((("*" | "/" | "%") unary_expr))*
        mark = self._mark()
        if (
            (first := self.unary_expr())
            and
            (rest := self._loop0_55(),)
        ):
            return fold_left ( first , [( op . string , e ) for op , e in rest] );
        self._reset(mark)
        return None;

    @memoize
    def unary_expr(self) -> Optional[object]:
        # unary_expr: decorator unary_expr | "-" unary_expr | "+" unary_expr | "~" unary_expr | 'not' unary_expr | power_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (dec := self.decorator())
            and
            (e := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Decorated ( dec , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("-"))
            and
            (e := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'neg' , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("+"))
            and
            (e := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'pos' , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("~"))
            and
            (e := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'inv' , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('not'))
            and
            (e := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return UnaryOp ( 'not' , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (power_expr := self.power_expr())
        ):
            return power_expr;
        self._reset(mark)
        return None;

    @memoize
    def power_expr(self) -> Optional[object]:
        # power_expr: postfix_expr "**" unary_expr | postfix_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (base := self.postfix_expr())
            and
            (self.expect("**"))
            and
            (exp := self.unary_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return BinOp ( '**' , base , exp , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (postfix_expr := self.postfix_expr())
        ):
            return postfix_expr;
        self._reset(mark)
        return None;

    @memoize
    def postfix_expr(self) -> Optional[object]:
        # postfix_expr: primary postfix_op*
        mark = self._mark()
        if (
            (first := self.primary())
            and
            (ops := self._loop0_56(),)
        ):
            return fold_postfix ( first , ops );
        self._reset(mark)
        return None;

    @memoize
    def postfix_op(self) -> Optional[object]:
        # postfix_op: call_op | index_op | attr_op | message_op | scope_op
        mark = self._mark()
        if (
            (call_op := self.call_op())
        ):
            return call_op;
        self._reset(mark)
        if (
            (index_op := self.index_op())
        ):
            return index_op;
        self._reset(mark)
        if (
            (attr_op := self.attr_op())
        ):
            return attr_op;
        self._reset(mark)
        if (
            (message_op := self.message_op())
        ):
            return message_op;
        self._reset(mark)
        if (
            (scope_op := self.scope_op())
        ):
            return scope_op;
        self._reset(mark)
        return None;

    @memoize
    def call_op(self) -> Optional[object]:
        # call_op: "(" arg_list? ")"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("("))
            and
            (args := self.arg_list(),)
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ( lambda node , a = ( args or [] ) : Call ( node , a , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) ) );
        self._reset(mark)
        return None;

    @memoize
    def index_op(self) -> Optional[object]:
        # index_op: "[" expression "]"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("["))
            and
            (e := self.expression())
            and
            (self.expect("]"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ( lambda node , e = e : Index ( node , e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) ) );
        self._reset(mark)
        return None;

    @memoize
    def attr_op(self) -> Optional[object]:
        # attr_op: "." NAME
        mark = self._mark()
        if (
            (self.expect("."))
            and
            (n := self.name())
        ):
            return ( lambda node , name = n . string , np = tok_pos ( n ) : Attr ( node , name , name_pos = np , pos = np ) );
        self._reset(mark)
        return None;

    @memoize
    def message_op(self) -> Optional[object]:
        # message_op: "!" NAME "(" arg_list? ")" | "!" NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("!"))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (args := self.arg_list(),)
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ( lambda node , name = n . string , a = ( args or [] ) , np = tok_pos ( n ) : Message ( node , name , a , name_pos = np , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) ) );
        self._reset(mark)
        if (
            (self.expect("!"))
            and
            (n := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ( lambda node , name = n . string , np = tok_pos ( n ) : Message ( node , name , None , name_pos = np , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) ) );
        self._reset(mark)
        return None;

    @memoize
    def scope_op(self) -> Optional[object]:
        # scope_op: "::" "$ast" | "::" NAME
        mark = self._mark()
        if (
            (self.expect("::"))
            and
            (self.expect("$ast"))
        ):
            return ( lambda node : AstRef ( node , "ast" , pos = getattr ( node , 'pos' , None ) ) );
        self._reset(mark)
        if (
            (self.expect("::"))
            and
            (n := self.name())
        ):
            return ( lambda node , name = n . string , np = tok_pos ( n ) : Scope ( node , name , name_pos = np , pos = np ) );
        self._reset(mark)
        return None;

    @memoize
    def arg_list(self) -> Optional[object]:
        # arg_list: argument (("," argument))*
        mark = self._mark()
        if (
            (first := self.argument())
            and
            (rest := self._loop0_57(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def argument(self) -> Optional[object]:
        # argument: NAME "=" assign_expr | "**" assign_expr | "*" assign_expr | assign_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (n := self.name())
            and
            (self.expect("="))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Kwarg ( n . string , e , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("**"))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SpreadKw ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("*"))
            and
            (e := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SpreadPos ( e , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (e := self.assign_expr())
        ):
            return e;
        self._reset(mark)
        return None;

    @memoize
    def primary(self) -> Optional[object]:
        # primary: literal | ellipsis_literal | message_tuple_expr | paren_or_tuple | array_literal | pair_literal | dict_literal | 'this' | 'super' "(" arg_list? ")" | 'defined' "(" literal ")" | lambda_expr | do_expr | NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (literal := self.literal())
        ):
            return literal;
        self._reset(mark)
        if (
            (ellipsis_literal := self.ellipsis_literal())
        ):
            return ellipsis_literal;
        self._reset(mark)
        if (
            (message_tuple_expr := self.message_tuple_expr())
        ):
            return message_tuple_expr;
        self._reset(mark)
        if (
            (paren_or_tuple := self.paren_or_tuple())
        ):
            return paren_or_tuple;
        self._reset(mark)
        if (
            (array_literal := self.array_literal())
        ):
            return array_literal;
        self._reset(mark)
        if (
            (pair_literal := self.pair_literal())
        ):
            return pair_literal;
        self._reset(mark)
        if (
            (dict_literal := self.dict_literal())
        ):
            return dict_literal;
        self._reset(mark)
        if (
            (self.expect('this'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return ThisRef ( pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('super'))
            and
            (self.expect("("))
            and
            (args := self.arg_list(),)
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return SuperCall ( args or [] , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('defined'))
            and
            (self.expect("("))
            and
            (s := self.literal())
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Defined ( s , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (lambda_expr := self.lambda_expr())
        ):
            return lambda_expr;
        self._reset(mark)
        if (
            (do_expr := self.do_expr())
        ):
            return do_expr;
        self._reset(mark)
        if (
            (name := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Name ( name . string , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def literal(self) -> Optional[object]:
        # literal: NUMBER | STRING | 'true' | 'false'
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (number := self.number())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Num ( number . string , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (string := self.string())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return make_string_literal ( string . string , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('true'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Bool ( True , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect('false'))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Bool ( False , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def ellipsis_literal(self) -> Optional[object]:
        # ellipsis_literal: "..."
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("..."))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return EllipsisExpr ( pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def array_literal(self) -> Optional[object]:
        # array_literal: "[" expr_list? "]"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("["))
            and
            (items := self.expr_list(),)
            and
            (self.expect("]"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Array ( items or [] , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def pair_literal(self) -> Optional[object]:
        # pair_literal: "$" "[" pair_body? "]"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("$"))
            and
            (self.expect("["))
            and
            (body := self.pair_body(),)
            and
            (self.expect("]"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Pair ( body or [] , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def pair_body(self) -> Optional[object]:
        # pair_body: assign_expr (("," assign_expr))*
        mark = self._mark()
        if (
            (first := self.assign_expr())
            and
            (rest := self._loop0_58(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def paren_or_tuple(self) -> Optional[object]:
        # paren_or_tuple: "(" ")" | "(" assign_expr "," ")" | "(" assign_expr (("," assign_expr))+ ","? ")" | "(" assign_expr ")"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("("))
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Tuple ( [] , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("("))
            and
            (first := self.assign_expr())
            and
            (self.expect(","))
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return make_tuple ( first , [] , "," , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("("))
            and
            (first := self.assign_expr())
            and
            (rest := self._loop1_59())
            and
            (trailing := self.expect(","),)
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return make_tuple ( first , tail ( rest ) , trailing , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("("))
            and
            (e := self.assign_expr())
            and
            (self.expect(")"))
        ):
            return e;
        self._reset(mark)
        return None;

    @memoize
    def dict_literal(self) -> Optional[object]:
        # dict_literal: "{" dict_body? "}"
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("{"))
            and
            (body := self.dict_body(),)
            and
            (self.expect("}"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Dict ( body or [] , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def dict_body(self) -> Optional[object]:
        # dict_body: dict_pair (("," dict_pair))*
        mark = self._mark()
        if (
            (first := self.dict_pair())
            and
            (rest := self._loop0_60(),)
        ):
            return [first] + tail ( rest );
        self._reset(mark)
        return None;

    @memoize
    def dict_pair(self) -> Optional[object]:
        # dict_pair: assign_expr ":" assign_expr
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (k := self.assign_expr())
            and
            (self.expect(":"))
            and
            (v := self.assign_expr())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return DictEntry ( k , v , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def message_tuple_expr(self) -> Optional[object]:
        # message_tuple_expr: "(" expr_list ")" "!" NAME "(" arg_list? ")" | "(" expr_list ")" "!" NAME
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect("("))
            and
            (items := self.expr_list())
            and
            (self.expect(")"))
            and
            (self.expect("!"))
            and
            (n := self.name())
            and
            (self.expect("("))
            and
            (args := self.arg_list(),)
            and
            (self.expect(")"))
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return MessageTupleExpr ( items , n . string , args or [] , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        if (
            (self.expect("("))
            and
            (items := self.expr_list())
            and
            (self.expect(")"))
            and
            (self.expect("!"))
            and
            (n := self.name())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return MessageTupleExpr ( items , n . string , None , name_pos = tok_pos ( n ) , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def lambda_expr(self) -> Optional[object]:
        # lambda_expr: 'fn' "(" param_list? ")" block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('fn'))
            and
            (self.expect("("))
            and
            (params := self.param_list(),)
            and
            (self.expect(")"))
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Lambda ( params or [] , body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def do_expr(self) -> Optional[object]:
        # do_expr: 'do' block
        mark = self._mark()
        tok = self._tokenizer.peek()
        start_lineno, start_col_offset = tok.start
        if (
            (self.expect('do'))
            and
            (body := self.block())
        ):
            tok = self._tokenizer.get_last_non_whitespace_token()
            end_lineno, end_col_offset = tok.end
            return Do ( body , pos=(start_lineno, start_col_offset, end_lineno, end_col_offset) );
        self._reset(mark)
        return None;

    @memoize
    def _loop0_1(self) -> Optional[Any]:
        # _loop0_1: stmt_item
        mark = self._mark()
        children = []
        while (
            (stmt_item := self.stmt_item())
        ):
            children.append(stmt_item)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_2(self) -> Optional[Any]:
        # _loop0_2: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_3(self) -> Optional[Any]:
        # _loop0_3: stmt_item
        mark = self._mark()
        children = []
        while (
            (stmt_item := self.stmt_item())
        ):
            children.append(stmt_item)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_4(self) -> Optional[Any]:
        # _loop0_4: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_5(self) -> Optional[Any]:
        # _loop0_5: brace_stmt_item
        mark = self._mark()
        children = []
        while (
            (brace_stmt_item := self.brace_stmt_item())
        ):
            children.append(brace_stmt_item)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_6(self) -> Optional[Any]:
        # _loop0_6: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop1_7(self) -> Optional[Any]:
        # _loop1_7: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_8(self) -> Optional[Any]:
        # _loop0_8: elif_clause
        mark = self._mark()
        children = []
        while (
            (elif_clause := self.elif_clause())
        ):
            children.append(elif_clause)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_9(self) -> Optional[Any]:
        # _loop0_9: ("," var_target)
        mark = self._mark()
        children = []
        while (
            (_tmp_61 := self._tmp_61())
        ):
            children.append(_tmp_61)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_10(self) -> Optional[Any]:
        # _tmp_10: "=" expr_list
        mark = self._mark()
        if (
            (literal := self.expect("="))
            and
            (expr_list := self.expr_list())
        ):
            return [literal, expr_list];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_11(self) -> Optional[Any]:
        # _tmp_11: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_12(self) -> Optional[Any]:
        # _loop0_12: ("," target)
        mark = self._mark()
        children = []
        while (
            (_tmp_62 := self._tmp_62())
        ):
            children.append(_tmp_62)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_13(self) -> Optional[Any]:
        # _loop0_13: target_index_suffix
        mark = self._mark()
        children = []
        while (
            (target_index_suffix := self.target_index_suffix())
        ):
            children.append(target_index_suffix)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop1_14(self) -> Optional[Any]:
        # _loop1_14: ("." NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_63 := self._tmp_63())
        ):
            children.append(_tmp_63)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop1_15(self) -> Optional[Any]:
        # _loop1_15: ("." NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_64 := self._tmp_64())
        ):
            children.append(_tmp_64)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_16(self) -> Optional[Any]:
        # _tmp_16: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_17(self) -> Optional[Any]:
        # _tmp_17: "=" assign_expr
        mark = self._mark()
        if (
            (literal := self.expect("="))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_18(self) -> Optional[Any]:
        # _loop0_18: ("::" NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_65 := self._tmp_65())
        ):
            children.append(_tmp_65)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_19(self) -> Optional[Any]:
        # _loop0_19: ("|" type_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_66 := self._tmp_66())
        ):
            children.append(_tmp_66)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_20(self) -> Optional[Any]:
        # _tmp_20: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_21(self) -> Optional[Any]:
        # _tmp_21: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_22(self) -> Optional[Any]:
        # _loop0_22: ("::" NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_67 := self._tmp_67())
        ):
            children.append(_tmp_67)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_23(self) -> Optional[Any]:
        # _loop0_23: ("," import_item)
        mark = self._mark()
        children = []
        while (
            (_tmp_68 := self._tmp_68())
        ):
            children.append(_tmp_68)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_24(self) -> Optional[Any]:
        # _loop0_24: ("," NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_69 := self._tmp_69())
        ):
            children.append(_tmp_69)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_25(self) -> Optional[Any]:
        # _tmp_25: "->" ann_type
        mark = self._mark()
        if (
            (literal := self.expect("->"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_26(self) -> Optional[Any]:
        # _tmp_26: "->" ann_type
        mark = self._mark()
        if (
            (literal := self.expect("->"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_27(self) -> Optional[Any]:
        # _loop0_27: ("," NAME)
        mark = self._mark()
        children = []
        while (
            (_tmp_70 := self._tmp_70())
        ):
            children.append(_tmp_70)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_28(self) -> Optional[Any]:
        # _loop0_28: ("," param)
        mark = self._mark()
        children = []
        while (
            (_tmp_71 := self._tmp_71())
        ):
            children.append(_tmp_71)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_29(self) -> Optional[Any]:
        # _tmp_29: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_30(self) -> Optional[Any]:
        # _tmp_30: "=" assign_expr
        mark = self._mark()
        if (
            (literal := self.expect("="))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_31(self) -> Optional[Any]:
        # _tmp_31: "->" ann_type
        mark = self._mark()
        if (
            (literal := self.expect("->"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_32(self) -> Optional[Any]:
        # _tmp_32: "->" ann_type
        mark = self._mark()
        if (
            (literal := self.expect("->"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_33(self) -> Optional[Any]:
        # _loop0_33: class_member_item
        mark = self._mark()
        children = []
        while (
            (class_member_item := self.class_member_item())
        ):
            children.append(class_member_item)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_34(self) -> Optional[Any]:
        # _loop0_34: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_35(self) -> Optional[Any]:
        # _loop0_35: class_member_brace
        mark = self._mark()
        children = []
        while (
            (class_member_brace := self.class_member_brace())
        ):
            children.append(class_member_brace)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_36(self) -> Optional[Any]:
        # _loop0_36: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_37(self) -> Optional[Any]:
        # _loop0_37: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_38(self) -> Optional[Any]:
        # _loop0_38: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_39(self) -> Optional[Any]:
        # _loop0_39: NEWLINE
        mark = self._mark()
        children = []
        while (
            (_newline := self.expect('NEWLINE'))
        ):
            children.append(_newline)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_40(self) -> Optional[Any]:
        # _tmp_40: ":" ann_type
        mark = self._mark()
        if (
            (literal := self.expect(":"))
            and
            (ann_type := self.ann_type())
        ):
            return [literal, ann_type];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_41(self) -> Optional[Any]:
        # _tmp_41: "=" assign_expr
        mark = self._mark()
        if (
            (literal := self.expect("="))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_42(self) -> Optional[Any]:
        # _tmp_42: 'with' slot_options
        mark = self._mark()
        if (
            (literal := self.expect('with'))
            and
            (slot_options := self.slot_options())
        ):
            return [literal, slot_options];
        self._reset(mark)
        return None;

    @memoize
    def _loop0_43(self) -> Optional[Any]:
        # _loop0_43: slot_option_brace
        mark = self._mark()
        children = []
        while (
            (slot_option_brace := self.slot_option_brace())
        ):
            children.append(slot_option_brace)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_44(self) -> Optional[Any]:
        # _tmp_44: 'setter' | 'getter'
        mark = self._mark()
        if (
            (literal := self.expect('setter'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect('getter'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_45(self) -> Optional[Any]:
        # _tmp_45: 'setter' | 'getter'
        mark = self._mark()
        if (
            (literal := self.expect('setter'))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect('getter'))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _loop0_46(self) -> Optional[Any]:
        # _loop0_46: ("," assign_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_72 := self._tmp_72())
        ):
            children.append(_tmp_72)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_47(self) -> Optional[Any]:
        # _loop0_47: ('or' and_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_73 := self._tmp_73())
        ):
            children.append(_tmp_73)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_48(self) -> Optional[Any]:
        # _loop0_48: ('and' not_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_74 := self._tmp_74())
        ):
            children.append(_tmp_74)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_49(self) -> Optional[Any]:
        # _loop0_49: ("|" type_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_75 := self._tmp_75())
        ):
            children.append(_tmp_75)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_50(self) -> Optional[Any]:
        # _loop0_50: ("|" bitwise_xor_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_76 := self._tmp_76())
        ):
            children.append(_tmp_76)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_51(self) -> Optional[Any]:
        # _loop0_51: ("^" bitwise_and_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_77 := self._tmp_77())
        ):
            children.append(_tmp_77)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_52(self) -> Optional[Any]:
        # _loop0_52: ("&" shift_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_78 := self._tmp_78())
        ):
            children.append(_tmp_78)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_53(self) -> Optional[Any]:
        # _loop0_53: (("<<" | ">>") additive_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_79 := self._tmp_79())
        ):
            children.append(_tmp_79)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_54(self) -> Optional[Any]:
        # _loop0_54: (("+" | "-") mult_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_80 := self._tmp_80())
        ):
            children.append(_tmp_80)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_55(self) -> Optional[Any]:
        # _loop0_55: (("*" | "/" | "%") unary_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_81 := self._tmp_81())
        ):
            children.append(_tmp_81)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_56(self) -> Optional[Any]:
        # _loop0_56: postfix_op
        mark = self._mark()
        children = []
        while (
            (postfix_op := self.postfix_op())
        ):
            children.append(postfix_op)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_57(self) -> Optional[Any]:
        # _loop0_57: ("," argument)
        mark = self._mark()
        children = []
        while (
            (_tmp_82 := self._tmp_82())
        ):
            children.append(_tmp_82)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_58(self) -> Optional[Any]:
        # _loop0_58: ("," assign_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_83 := self._tmp_83())
        ):
            children.append(_tmp_83)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop1_59(self) -> Optional[Any]:
        # _loop1_59: ("," assign_expr)
        mark = self._mark()
        children = []
        while (
            (_tmp_84 := self._tmp_84())
        ):
            children.append(_tmp_84)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _loop0_60(self) -> Optional[Any]:
        # _loop0_60: ("," dict_pair)
        mark = self._mark()
        children = []
        while (
            (_tmp_85 := self._tmp_85())
        ):
            children.append(_tmp_85)
            mark = self._mark()
        self._reset(mark)
        return children;

    @memoize
    def _tmp_61(self) -> Optional[Any]:
        # _tmp_61: "," var_target
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (var_target := self.var_target())
        ):
            return [literal, var_target];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_62(self) -> Optional[Any]:
        # _tmp_62: "," target
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (target := self.target())
        ):
            return [literal, target];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_63(self) -> Optional[Any]:
        # _tmp_63: "." NAME
        mark = self._mark()
        if (
            (literal := self.expect("."))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_64(self) -> Optional[Any]:
        # _tmp_64: "." NAME
        mark = self._mark()
        if (
            (literal := self.expect("."))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_65(self) -> Optional[Any]:
        # _tmp_65: "::" NAME
        mark = self._mark()
        if (
            (literal := self.expect("::"))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_66(self) -> Optional[Any]:
        # _tmp_66: "|" type_expr
        mark = self._mark()
        if (
            (literal := self.expect("|"))
            and
            (type_expr := self.type_expr())
        ):
            return [literal, type_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_67(self) -> Optional[Any]:
        # _tmp_67: "::" NAME
        mark = self._mark()
        if (
            (literal := self.expect("::"))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_68(self) -> Optional[Any]:
        # _tmp_68: "," import_item
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (import_item := self.import_item())
        ):
            return [literal, import_item];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_69(self) -> Optional[Any]:
        # _tmp_69: "," NAME
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_70(self) -> Optional[Any]:
        # _tmp_70: "," NAME
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (name := self.name())
        ):
            return [literal, name];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_71(self) -> Optional[Any]:
        # _tmp_71: "," param
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (param := self.param())
        ):
            return [literal, param];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_72(self) -> Optional[Any]:
        # _tmp_72: "," assign_expr
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_73(self) -> Optional[Any]:
        # _tmp_73: 'or' and_expr
        mark = self._mark()
        if (
            (literal := self.expect('or'))
            and
            (and_expr := self.and_expr())
        ):
            return [literal, and_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_74(self) -> Optional[Any]:
        # _tmp_74: 'and' not_expr
        mark = self._mark()
        if (
            (literal := self.expect('and'))
            and
            (not_expr := self.not_expr())
        ):
            return [literal, not_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_75(self) -> Optional[Any]:
        # _tmp_75: "|" type_expr
        mark = self._mark()
        if (
            (literal := self.expect("|"))
            and
            (type_expr := self.type_expr())
        ):
            return [literal, type_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_76(self) -> Optional[Any]:
        # _tmp_76: "|" bitwise_xor_expr
        mark = self._mark()
        if (
            (literal := self.expect("|"))
            and
            (bitwise_xor_expr := self.bitwise_xor_expr())
        ):
            return [literal, bitwise_xor_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_77(self) -> Optional[Any]:
        # _tmp_77: "^" bitwise_and_expr
        mark = self._mark()
        if (
            (literal := self.expect("^"))
            and
            (bitwise_and_expr := self.bitwise_and_expr())
        ):
            return [literal, bitwise_and_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_78(self) -> Optional[Any]:
        # _tmp_78: "&" shift_expr
        mark = self._mark()
        if (
            (literal := self.expect("&"))
            and
            (shift_expr := self.shift_expr())
        ):
            return [literal, shift_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_79(self) -> Optional[Any]:
        # _tmp_79: ("<<" | ">>") additive_expr
        mark = self._mark()
        if (
            (_tmp_86 := self._tmp_86())
            and
            (additive_expr := self.additive_expr())
        ):
            return [_tmp_86, additive_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_80(self) -> Optional[Any]:
        # _tmp_80: ("+" | "-") mult_expr
        mark = self._mark()
        if (
            (_tmp_87 := self._tmp_87())
            and
            (mult_expr := self.mult_expr())
        ):
            return [_tmp_87, mult_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_81(self) -> Optional[Any]:
        # _tmp_81: ("*" | "/" | "%") unary_expr
        mark = self._mark()
        if (
            (_tmp_88 := self._tmp_88())
            and
            (unary_expr := self.unary_expr())
        ):
            return [_tmp_88, unary_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_82(self) -> Optional[Any]:
        # _tmp_82: "," argument
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (argument := self.argument())
        ):
            return [literal, argument];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_83(self) -> Optional[Any]:
        # _tmp_83: "," assign_expr
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_84(self) -> Optional[Any]:
        # _tmp_84: "," assign_expr
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (assign_expr := self.assign_expr())
        ):
            return [literal, assign_expr];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_85(self) -> Optional[Any]:
        # _tmp_85: "," dict_pair
        mark = self._mark()
        if (
            (literal := self.expect(","))
            and
            (dict_pair := self.dict_pair())
        ):
            return [literal, dict_pair];
        self._reset(mark)
        return None;

    @memoize
    def _tmp_86(self) -> Optional[Any]:
        # _tmp_86: "<<" | ">>"
        mark = self._mark()
        if (
            (literal := self.expect("<<"))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect(">>"))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_87(self) -> Optional[Any]:
        # _tmp_87: "+" | "-"
        mark = self._mark()
        if (
            (literal := self.expect("+"))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect("-"))
        ):
            return literal;
        self._reset(mark)
        return None;

    @memoize
    def _tmp_88(self) -> Optional[Any]:
        # _tmp_88: "*" | "/" | "%"
        mark = self._mark()
        if (
            (literal := self.expect("*"))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect("/"))
        ):
            return literal;
        self._reset(mark)
        if (
            (literal := self.expect("%"))
        ):
            return literal;
        self._reset(mark)
        return None;

    KEYWORDS = ('and', 'break', 'catch', 'class', 'co', 'continue', 'defer', 'defined', 'do', 'elif', 'else', 'false', 'fn', 'for', 'from', 'getter', 'if', 'import', 'in', 'is', 'not', 'or', 'pass', 'return', 'setter', 'slot', 'static', 'super', 'this', 'true', 'try', 'undefined', 'var', 'while', 'with', 'yield')
    SOFT_KEYWORDS = ('as', 'error', 'except', 'on')


if __name__ == '__main__':
    from pegen.parser import simple_parser_main
    simple_parser_main(GeneratedParser)
