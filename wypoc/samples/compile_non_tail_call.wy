import native

fn inc(a: int) -> int:
    return a + 1

fn double(a: int) -> int:
    return a * 2

fn combo(x: int) -> int:
    y: int = inc(x)
    z: int = double(y)
    return z + 1

fn log_and_use(x: int) -> int:
    inc(x)
    return x
