import native

native::block('HEADER, $[], $[], R"C(
#include <stdio.h>
)C")

fn combine(a: int, b: int, c: int) -> int:
    var x: int
    var y: int
    native::block('BODY, $['a, 'b, 'c], $['x, 'y], R"C(
        x = a + b;
        y = b - c;
    )C")
    return x + y
