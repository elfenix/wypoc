"""wyrm's DAP (Debug Adapter Protocol) server - see debugger.py (the part
that understands wyrm: call stack, breakpoints, stepping) and protocol.py/
server.py (the wire format and request handling)."""
